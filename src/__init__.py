"""Top-level package for the school management app.

This file makes the `src` directory a Python package so that
relative imports like `from .backend import routers` work reliably
when running the app with `uvicorn src.app:app`.
"""
